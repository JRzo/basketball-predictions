<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= predict.user %>'s NBA Prediction</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card shadow-sm">
            <div class="card-header text-center bg-dark text-white">
                <h2 class="mb-0"><%= predict.user %>'s NBA Playoffs Picks</h2>
            </div>

            <div class="card-body">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">
                        🏆 <strong>Champion:</strong> <%= predict.champion %>
                    </li>
                    <li class="list-group-item">
                        🥈 <strong>Runner-Up:</strong> <%= predict.runnerUp %>
                    </li>
                    <li class="list-group-item">
                        🌟 <strong>Finals MVP:</strong> <%= predict.finalsMVP %>
                    </li>
                    <li class="list-group-item">
                        🚨 <strong>Dark Horse:</strong> <%= predict.darkHorse %>
                    </li>
                    <li class="list-group-item">
                        📊 <strong>Confidence:</strong> <%= predict.confidenceLevel %> / 10
                    </li>
                    <li class="list-group-item">
                        🗓️ <strong>Submitted on:</strong> <%= predict.predictionDate.toLocaleDateString() %>
                    </li>
                </ul>
            </div>

            <div class="card-footer d-flex justify-content-between">
                <a href="/edit/<%= predict._id %>" class="btn btn-outline-warning">✏️ Edit</a>
                <a href="/" class="btn btn-outline-primary">⬅ Back to All Predictions</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
