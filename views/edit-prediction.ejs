<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your NBA Playoff Pick</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4 text-center">✏️ Update Your Playoff Prediction</h1>

        <!-- Edit form pre-filled with existing prediction data -->
        <form action="/edit/<%= prediction._id %>" method="POST" class="needs-validation" novalidate>
            <div class="mb-3">
                <label for="user" class="form-label">Name</label>
                <input type="text" class="form-control" id="user" name="user" value="<%= prediction.user %>" required>
            </div>

            <div class="mb-3">
                <label for="champion" class="form-label">Predicted Champion</label>
                <input type="text" class="form-control" id="champion" name="champion" value="<%= prediction.champion %>" required>
            </div>

            <div class="mb-3">
                <label for="runnerUp" class="form-label">Runner-Up</label>
                <input type="text" class="form-control" id="runnerUp" name="runnerUp" value="<%= prediction.runnerUp %>" required>
            </div>

            <div class="mb-3">
                <label for="finalsMVP" class="form-label">Finals MVP</label>
                <input type="text" class="form-control" id="finalsMVP" name="finalsMVP" value="<%= prediction.finalsMVP %>" required>
            </div>

            <div class="mb-3">
                <label for="darkHorse" class="form-label">Dark Horse Team</label>
                <input type="text" class="form-control" id="darkHorse" name="darkHorse" value="<%= prediction.darkHorse %>" required>
            </div>

            <div class="mb-4">
                <label for="confidenceLevel" class="form-label">Confidence Level (1–10)</label>
                <input type="number" class="form-control" id="confidenceLevel" name="confidenceLevel" min="1" max="10" value="<%= prediction.confidenceLevel %>" required>
            </div>

            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-warning">💾 Save Changes</button>
                <a href="/" class="btn btn-outline-secondary">⬅ Cancel</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap validation script -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        (() => {
            'use strict';
            const forms = document.querySelectorAll('.needs-validation');
            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                    if (!form.checkValidity()) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        })();
    </script>
</body>
</html>
